
# BEGIN WordPress
# Las directivas (líneas) entre `BEGIN WordPress` y `END WordPress` se generan dinámicamente, y solo se deberían modificar mediante filtros de WordPress.
# Cualquier cambio en las directivas que hay entre esos marcadores se sobrescribirá.
<IfModule mod_rewrite.c>
RewriteEngine On
RewriteBase /el_muro/
RewriteRule ^index\.php$ - [L]
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d
RewriteRule . /el_muro/index.php [L]
</IfModule>

# END WordPress
# BEGIN HttpHeaders
# Las directivas (líneas) entre `BEGIN HttpHeaders` y `END HttpHeaders` se generan dinámicamente, y solo se deberían modificar mediante filtros de WordPress.
# Cualquier cambio en las directivas que hay entre esos marcadores se sobrescribirá.
# END HttpHeaders
# BEGIN HttpHeadersAuth
# Las directivas (líneas) entre `BEGIN HttpHeadersAuth` y `END HttpHeadersAuth` se generan dinámicamente, y solo se deberían modificar mediante filtros de WordPress.
# Cualquier cambio en las directivas que hay entre esos marcadores se sobrescribirá.
# END HttpHeadersAuth
# BEGIN HttpHeadersCompression
# Las directivas (líneas) entre `BEGIN HttpHeadersCompression` y `END HttpHeadersCompression` se generan dinámicamente, y solo se deberían modificar mediante filtros de WordPress.
# Cualquier cambio en las directivas que hay entre esos marcadores se sobrescribirá.
# END HttpHeadersCompression
# BEGIN HttpHeadersContentType
# Las directivas (líneas) entre `BEGIN HttpHeadersContentType` y `END HttpHeadersContentType` se generan dinámicamente, y solo se deberían modificar mediante filtros de WordPress.
# Cualquier cambio en las directivas que hay entre esos marcadores se sobrescribirá.
# END HttpHeadersContentType
# BEGIN HttpHeadersExpires
# Las directivas (líneas) entre `BEGIN HttpHeadersExpires` y `END HttpHeadersExpires` se generan dinámicamente, y solo se deberían modificar mediante filtros de WordPress.
# Cualquier cambio en las directivas que hay entre esos marcadores se sobrescribirá.
# END HttpHeadersExpires
# BEGIN HttpHeadersCookieSecurity
# Las directivas (líneas) entre `BEGIN HttpHeadersCookieSecurity` y `END HttpHeadersCookieSecurity` se generan dinámicamente, y solo se deberían modificar mediante filtros de WordPress.
# Cualquier cambio en las directivas que hay entre esos marcadores se sobrescribirá.
# END HttpHeadersCookieSecurity
# BEGIN HttpHeadersTiming
# Las directivas (líneas) entre `BEGIN HttpHeadersTiming` y `END HttpHeadersTiming` se generan dinámicamente, y solo se deberían modificar mediante filtros de WordPress.
# Cualquier cambio en las directivas que hay entre esos marcadores se sobrescribirá.
# END HttpHeadersTiming